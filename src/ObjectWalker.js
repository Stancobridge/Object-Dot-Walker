class ObjectWalker {
  constructor(item, cb) {
    //
    if (
      Array.isArray(item) &&
      typeof item[0] === "object" &&
      item[0] !== null
    ) {
      // item is an array with object
      
    }
  }
}
